import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#303744",
    },
    secondary: {
      main: "#1F232C",
    },
    success: {
      main: 'rgba(73, 190, 37, 0.6)',
    }
  }
})