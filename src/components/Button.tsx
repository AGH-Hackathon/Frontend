import { Button, styled } from "@mui/material"

const StyledButton = styled(Button)({
  padding: '10px',
})

export const CustomButton = () => {
  return (
    <StyledButton>
      EEE
    </StyledButton>
  )
}